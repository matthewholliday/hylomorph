You are authoring **layer 3 (tasks)** of a harness spec. Your job is to write
exactly one file — `.specs/{spec_name}/3-tasks.jsonl` — and then stop. Do not
edit the requirements or design, and do not implement any code. The harness will
reject any write outside that one file.

The requirements and design layers already exist and are your inputs. Derive the
task breakdown from them; do not introduce work that neither covers.

---

## Requirements (layer 1 — read-only input)

```json
{requirements}
```

---

## Design (layer 2 — read-only input)

```markdown
{design}
```

---

## File to write: `.specs/{spec_name}/3-tasks.jsonl`

One JSON object per line. Rules:
- Tasks are the unit the loop runs one at a time; keep each focused and
  completable in a single agent session.
- `id` is `T-001`, `T-002`, … — stable and unique within the spec.
- `requirements`: the REQ-### ids (from layer 1) this task helps satisfy. Every
  requirement should be covered by at least one task.
- `priority`: lower number runs sooner. Start at 1.
- `depends_on`: task ids that must be `done` before this task starts; use
  sparingly and only when truly sequential.
- `hooks`: the blocking hooks that must pass for this task to count as done.
  Omit the field to use the project default. Common values: `["run_build",
  "run_lint", "run_unit_tests"]`.
- `acceptance`: 1–4 concrete, human-readable done criteria that match the
  requirement's acceptance_criteria.
- `files_hint`: paths or globs the agent is likely to touch — helps the agent
  scope its work.
- `status` must be `"todo"` for every task.
- Timestamps: use `"2026-01-01T00:00:00Z"` as a placeholder.

Schema (one object per line — no trailing comma, one per line):

```
{"id":"T-001","spec":"{spec_name}","title":"<imperative verb phrase>","requirements":["REQ-001"],"status":"todo","priority":1,"depends_on":[],"hooks":["run_build","run_lint","run_unit_tests"],"acceptance":["<assertion>"],"files_hint":["<path>"],"attempts":0,"notes":"","created_at":"2026-01-01T00:00:00Z","updated_at":"2026-01-01T00:00:00Z"}
```

Every `requirements` entry must reference a REQ-### id that exists in layer 1,
and every `depends_on` entry must reference a task id you define in this file.
When done, stop — code is generated by `harness build {spec_name}`.
